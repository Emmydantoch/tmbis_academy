{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "routes": [
    {
      "src": "/api/(.*)",
      "headers": {
        "Cache-Control": "no-store, no-cache, must-revalidate"
      }
    }
  ]
}